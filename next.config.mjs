/** @type {import('next').NextConfig} */
const nextConfig = {
    // 开启静态导出
    output: 'export',
    // 如果你想所有页面都输出到 /page-name/index.html：
    // trailingSlash: true,
  };
  
  export default nextConfig;
  